# PillChecker Web App — environment variables
# Copy this file to .env and fill in your values.
# .env is gitignored; .env.example is committed.

# URL of the PillChecker backend API
# Local dev: run `docker compose up` in the backend repo
# VITE_API_URL=http://localhost:8000
VITE_API_URL=http://localhost:8000
